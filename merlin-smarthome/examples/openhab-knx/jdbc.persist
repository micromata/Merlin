/* Generated by SmartHomeConfig: $data.dateTime */
$data.config["Persistence-Strategies"]

Items {
#foreach( $persistancy in $data.knxPersistencies )
  // Items for the persistency strategy: $persistancy
    #set($separator="  ")#foreach( $thing in $data.knxPersistencyThingMap[$persistancy] )$separator$thing.Id #set($separator=", ")#end: strategy = $persistancy
#end
}
